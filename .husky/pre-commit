#!/bin/sh

# Run PRD interface validator
echo "Validating PRD interface versions..."
cd portal && npm run validate:prd
if [ $? -ne 0 ]; then
  echo "PRD validation failed. Update stale PRDs before committing."
  exit 1
fi

echo "PRD validation passed."
