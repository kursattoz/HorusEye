# BL-24 Phase A — FastAPI scaffold (see PRD-013 §12)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
python-multipart>=0.0.9
numpy>=1.26.0
opencv-python-headless>=4.9.0
PyYAML>=6.0.0
httpx>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.23.0

# BL-42 — YOLOv8n COCO pre-trained inference (PRD-013 §4.1)
ultralytics>=8.3.0

# BL-182 (Sprint 7) — BoT-SORT multi-object tracker (PRD-013 §4.2)
boxmot>=11.0.0
lapx>=0.5.0

# BL-185 (Sprint 7) — Supabase service-role client for incident persistence
supabase>=2.0.0

# BL-194 (Sprint 8) — MediaPipe Face Mesh for gaze + head-pose (PRD-013 §4.3).
# Pinned to 0.10.14+ for Python 3.12 compatibility; OS deps (libGL,
# libglib2.0-0) already satisfied by the opencv-python-headless layer.
mediapipe>=0.10.14
